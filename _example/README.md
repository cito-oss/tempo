# Examples

Clone this repository:

```bash
git clone https://github.com/cito-oss/tempo.git
cd tempo/_example
```

Start Temporal:

```bash
docker compose up -d --wait
```

Run the tests:

```bash
go run main.go
```
